#include <stdio.h>
#include <string.h> // strcmp(string compare)
#include <stdbool.h> // bool function
// #define MAX_LENGTH 50

int main() {
    int numbers[] = {20, 500, 10, 5, 100, 1, 50};
    int arraylength = sizeof(numbers);
    printf("Type a number: %i\n", arraylength);

    return 0;
}